{
  "name": "hule-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter @hule/web dev",
    "dev:api": "pnpm --filter @hule/api dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @hule/web build",
    "build:api": "pnpm --filter @hule/api build",
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  }
}
