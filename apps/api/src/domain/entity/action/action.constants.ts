export enum EActionEvent {
  TaskAfterUpdate = 'task.after-update',
}

export enum EActionConditionOp {
  ChangedTo = 'changed-to',
  Equals = 'equals',
}

export enum EActionEffectKind {
  SpawnTaskFromTemplate = 'spawn-task-from-template',
}
